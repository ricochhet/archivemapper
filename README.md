## archivemapper
Compare files from archives against existing files.

### Building
- Run `make build-<platform>` e.g. `make build-windows`
- (or) Run `go build -trimpath -ldflags="-s -w" .`

### Usage
- Run `archivemapper -h` to view a list of commands.

### License
See [LICENSE](./LICENSE) file.
